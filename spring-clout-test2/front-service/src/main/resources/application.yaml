spring:
  application:
    name: front-service
logging:
  level:
    root: INFO
server:
  port: 6783
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:6781/eureka}
      fetchRegistry: false