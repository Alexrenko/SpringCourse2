spring:
  application:
    name: slow-service
server:
  port: 0
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:6781/eureka}
      fetchRegistry: false